import React from 'react';
import ReactDOM from 'react-dom/client';

import ReactTreeUI, { TreeBuilder } from "../index";

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);

root.render(
    <></>
);